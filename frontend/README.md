# Frontend

## Getting Started

Compiling:

```
npm install
npm run build
```

Run the development server:

```bash
npm run dev
```

Then open http://localhost:3000 with your browser.

Run the tests:

```bash
npx cypress open
```

Run the tests (headless):

```bash
npx cypress run
```
